services:
  supervisor-bot:
    build: .
    container_name: supervisor-bot
    restart: unless-stopped
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - ADMIN_ID=${ADMIN_ID}
    volumes:
      - ./reports.db:/app/reports.db
    env_file:
      - .env
